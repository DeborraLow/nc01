{% from "components/project-overview/project-overview-item.njk" import projectOverview as projectOverview %}

<ul class="project-overview js-content-height">
    {% for project in projects %}
        {{ projectOverview(project.author_overview, 
        				   project.title,
                           project.title_in, 
        				   project.cover_text, 
        				   project.cover_text_in, 
        				   project.cover_img, 
        				   loop.index, 
        				   project.href) 
        }}
    {% endfor %}
</ul>
